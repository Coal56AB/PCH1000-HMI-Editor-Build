# Retained to keep full repository archives compatible. Never republish the old debug APK.
name: Legacy publication disabled
on:
  workflow_dispatch:
permissions:
  contents: read
jobs:
  obsolete:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "Use Build APK: old debug releases cannot be published by this workflow."
          exit 1
