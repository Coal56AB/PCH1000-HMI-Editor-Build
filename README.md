## Вопросы по исходникам (1.2.5)

Включи Codex-бета в настройках, выбери свой C-репозиторий и ветку в GitHub, затем открой CODEX → «Спросить». Первый вопрос создаёт Issue с контекстом выбранного элемента и SHA коммита; следующие сообщения продолжают тот же диалог. Файлы из приложения при этом не выгружаются и не коммитятся. Вопрос относится к исходникам, уже находящимся в выбранном репозитории.

Codex должен быть подключён к этому репозиторию. Приложение отправляет @codex с явной инструкцией только ответить, без правок; это инструкция агенту, а не изменение его разрешений на стороне GitHub. Само приложение в режиме вопросов не изменяет файлы или ветки. Текст ответа отображается, когда он появляется в GitHub; если бот вернул ссылку на облачный чат, её можно открыть из сообщения. При отсутствии реакции проверь подключение Codex. Обсуждение имеет видимость выбранного репозитория.

Ответы обновляются каждые 15 секунд, пока открыта панель вопросов. Можно обновить вручную, начать новый диалог или открыть существующий Issue/PR по номеру. «Изменить C-код» остаётся отдельным режимом.

# PCH1000 HMI Editor — 1.2.4

Редактор интерфейса экранчика. C-код нужен для встраивания в микроконтроллер, который будет по нему всё отрисовывать.


## Что изменилось

- После генерации для GitHub push окно «47 из 47» закрывается. Очистка выполняется и при ошибке, симулятор возвращается в живой режим без перезапуска приложения.
- На верхней панели одна кнопка «Экспорт». В окне выбирается рабочая папка или ZIP; генерация начинается только по кнопке «Экспортировать». «Отмена» не запускает генерацию.
- «Откр.» убрана с верхней панели. Рабочая папка выбирается в меню «Проект»; импорт ранее сохранённого JSON/ZIP остаётся там же.
- Повторное нажатие push во время выполнения блокируется.

## Загрузка в GitHub и релиз

После merge или push в main workflow Build APK проверяет код, автоматически повышает versionName и Android versionCode, собирает подписанный APK и публикует новый релиз. Вручную менять номер для каждого merge не нужно. Проверки PR собирают только debug APK и не публикуют релизы.

Номер определяется историей main: первый коммит после базового `1e87aefb0b5a1d9b68d0063c3b17fea969b70bfe` получает 1.2.6 (versionCode 9), следующий — 1.2.7 (10), и так далее. Считается цепочка первых родителей: merge нескольких коммитов повышает номер один раз. При fast-forward/rebase или push нескольких коммитов возможен пропуск номеров. Повторная сборка того же коммита сохраняет версию и не перезаписывает опубликованный релиз. Незавершённая загрузка draft продолжается при повторном запуске. Поздно завершившаяся старая сборка не заменяет более новый latest.

Версия проставляется в рабочей копии `app/build.gradle` перед release-сборкой скриптом `python3 scripts/prepare_release.py stamp`; отдельный коммит с повышением версии не создаётся. Для ручной сборки из Git нужна полная история и тот же шаг. В полном ZIP исходников уже сохранён использованный при сборке `app/build.gradle` и `release-metadata.json`, поэтому архив содержит фактическую версию APK. Релиз включает APK, полный ZIP, метаданные версии/коммита и SHA256SUMS.txt. Публикация разрешена только после проверки подписи и совпадения версии APK с метаданными.

В режиме «Изменить C-код» приложение сначала создаёт ветку и сохраняет описание задачи в новом `.codex/tasks/hmi-….md`, затем открывает Draft PR и отправляет `@codex`. Это даёт PR начальное изменение, не перезаписывая пользовательские C-файлы. Повтор того же незавершённого запроса продолжает существующую ветку/PR; подробности ошибок GitHub показываются внизу окна.

Основной сценарий: `.github/workflows/build-apk.yml`. Исторический `publish-v1.2.2.yml` сохранён как отключённая заглушка, чтобы случайно не опубликовать несовместимый debug APK; новая публикация не зависит от старых run ID и срока хранения артефактов.

Подпись release постоянная: ключ восстанавливается из Actions Secret, сертификат сверяется с signing-certificate.sha256 до сборки и после подписи APK. Без правильного ключа публикация останавливается. Переход со старых debug APK требует однократной переустановки: их закрытые ключи не сохранились. Сначала сохрани проекты. После этого обновления устанавливаются поверх release с сохранением данных и стандартным подтверждением Android.

## Изменения 1.2.2

Client ID GitHub уже встроен: `Ov23liZhW4JlhvBxx6zs`. Это публичный идентификатор приложения, не токен доступа. Каждый пользователь входит в свой аккаунт.

Вход: «Войти через GitHub» → «Скопировать код и открыть GitHub» → вставить код на сайте и подтвердить доступ → вернуться в редактор. Если GitHub уже сообщил об успехе, при временной потере сети повторно вводить код не надо: редактор продолжает проверять подтверждение до истечения кода. При закрытии процесса Android незавершённую попытку нужно начать заново.

Используется Device Flow. Для обычного web OAuth входа без ручного одноразового кода GitHub OAuth Apps требует обмен с Client Secret; такой секрет нельзя встраивать в распространяемый APK. Сервер обмена в этот проект не добавлен.

Полный архив содержит Build Fix. Для сборки нужны JDK 17, Gradle 8.9 и Android SDK 35. GitHub Actions собирает `assembleRelease` с постоянным ключом. `assembleDebug` предназначен для разработки и использует отдельный applicationId с суффиксом `.debug`, чтобы не заменять основную установку.

# ПЧ-1000 HMI Editor для Android

Приложение для просмотра, проверки и редактирования интерфейса экранчика ПЧ-1000.
На выходе получается C-код для встраивания в микроконтроллер, который будет
отрисовывать интерфейс на реальном дисплее.

## Возможности

- живая интерактивная симуляция: вкладки, графики, клавиатуры и диалоги работают как в исходной HTML-версии;
- отдельный переход в режим редактирования без выбора одной из 47 QA-сцен вручную;
- внешние кнопки `ЗАРЯД / РАЗРЯД`, `ПУСК / СТОП` и цифровой энкодер с переключением по удержанию 0,5 с;
- максимальный целочисленный масштаб: один пиксель TFT отображается блоком 2×2, 3×3 и т. д. физических пикселей телефона;
- точный предпросмотр через исходный полосовой WASM/RGB565-рендерер;
- автоматическое определение текущего состояния; 47 контрольных сцен используются внутри экспортера;
- выбор элемента касанием;
- перемещение, изменение ширины/высоты, точные координаты и сетка;
- изменение собственного текста, размера и насыщенности шрифта;
- независимое пиксельное смещение текста по X/Y внутри блока без перемещения самого блока;
- точный дополнительный зазор после выбранного символа, например 1 px после `/`;
- цвета текста, фона, рамки, SVG-линии и SVG-заливки;
- глобальные изменения и отдельные переопределения для каждого сценария;
- undo/redo и автоматическое локальное сохранение;
- импорт JSON-проекта или ранее экспортированного ZIP;
- экспорт ZIP с JSON-проектом и готовым C99-рендерером;
- прямой экспорт тех же исходников обычными файлами в выбранную папку Android.
- постоянная рабочая папка: проект редактора сохраняется туда автоматически, а полный C-проект — по команде;
- вход в GitHub через OAuth Device Flow или personal access token, выбор/создание репозитория и ветки, pull, commit и push;
- закреплённый репозиторий обновлений APK через Releases (не связан с пользовательским C-проектом);
- необязательный режим `Codex — БЕТА`: отдельная ветка, Draft PR и задача через `@codex`;
- сравнение результата `БЫЛО / СТАЛО` и подсветка изменившихся пикселей;
- короткое обучение при первом запуске, которое можно повторить из настроек.

## GitHub

Разработчик один раз регистрирует GitHub OAuth App и включает Device Flow.
Публичный Client ID можно включить в APK через переменную окружения
`GITHUB_OAUTH_CLIENT_ID` или `gradle -PgithubOAuthClientId=… assembleDebug`.
Тогда пользователь видит кнопку входа через сайт GitHub без ввода Client ID.
Client secret для Device Flow не нужен и в APK не хранится.
В сборке без настроенного ID показана честная разовая инструкция, а не форма ввода логина.
Также доступен вход с fine-grained или classic personal access token. Токен шифруется
ключом Android Keystore.

Codex-режим по умолчанию выключен. После включения приложение создаёт отдельную ветку
и Draft PR, добавляет комментарий `@codex` и показывает текстовый дифф. Для визуального
сравнения ветка должна содержать синхронизированный `PCH1000_HMI_editor_project.json`.
Слияние выполняется только после явного нажатия пользователя.

Обновление APK проверяется по последнему GitHub Release репозитория
`Coal56AB/PCH1000-HMI-Editor-Build`, закреплённого в Android-коде. Авторизация не нужна.
Пользователь выбирает только репозиторий своего C-проекта; этот выбор не влияет на обновления APK.
Установка всегда подтверждается системным установщиком Android.

## Сборка

Откройте каталог в Android Studio (JDK 17, Android SDK 35) и выполните
`Build > Build APK(s)`, либо запустите `gradle assembleDebug`.

Готовый debug APK появится в `app/build/outputs/apk/debug/app-debug.apk`; это отдельное приложение для разработки. Публикуется только подписанный `app/build/outputs/apk/release/app-release.apk`.

## Экспорт из приложения

Кнопка `Экспорт` открывает выбор места сохранения и подтверждение. После подтверждения
редактор воспроизводит все контрольные состояния и применяет глобальные и сценарные правки.
При выборе ZIP предлагается сохранить `PCH1000_HMI_edited.zip`. В обоих вариантах сохраняются:

- `PCH1000_HMI_editor_project.json`;
- `PCH1000_HMI_C_Renderer_edited/src/hmi_scene_generated.c`;
- `PCH1000_HMI_C_Renderer_edited/include/hmi_scene_generated.h`;
- остальные заголовки, графический движок и динамический слой C-рендерера.

Вариант «В рабочую папку» сохраняет тот же полный набор без ZIP в папку из меню «Проект».
Одноимённые файлы обновляются. Если папка ещё не выбрана, генерация не начинается.

Приложение не сохраняет полноэкранные пиксельные кадры в C-коде. Экспорт состоит
из линий, прямоугольников, окружностей, треугольников и текстовых команд.
